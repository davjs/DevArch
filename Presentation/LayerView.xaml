<UserControl x:Class="Presentation.LayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation"
             mc:Ignorable="d"  d:DataContext="{d:DesignInstance local:LayerView}">

    <UserControl.Resources>
        <!-- Layer border style -->
        <Style x:Key="LayerBorderStyle" TargetType="{x:Type Border}">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="3"/>
            <Setter Property="BorderBrush" Value="#0F383E"/>
            <Setter Property="CornerRadius" Value="5"/>
        </Style>
    </UserControl.Resources>

    <Border x:Name="Border" Width="Auto" Style="{StaticResource LayerBorderStyle}" Background="#002B36">
        <DockPanel Width="Auto" >
            <TextBlock x:Name="NameBlock"  HorizontalAlignment="Center" Width="Auto" Padding="5" Foreground="#839496" DockPanel.Dock="Top" Text="{Binding LayerName}"/>
                <Grid x:Name="ChildHolder">
                </Grid>
            </DockPanel>
        </Border>

</UserControl>
